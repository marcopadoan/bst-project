Questo programma implementa un Binary Search Tree (BST) in C++.
Permette di:

- inserire valori nell’albero (in modo iterativo e ricorsivo)
- cercare un valore (iterativo e ricorsivo)
- gestire automaticamente la memoria grazie ai distruttori di Node e Bst
- cancellare un nodo dal BST
- scorrere la struttura in in-order, pre-order e post-order per visualizzare i valori